# OpenStreetMap-Folium
This Python script uses the OpenStreetMap API and 'folium' library to get geographic coordinates for a user-provided location. It creates an interactive map with a marker representing the location, saved as an HTML file for viewing.
